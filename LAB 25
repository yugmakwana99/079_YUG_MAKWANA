import serial import time

arduino = serial.Serial('COM4', 9600) time.sleep(2)

while True:
message = input("Enter message to send: ") arduino.write(message.encode()) print("Message sent:", message)

